# 日志分析报告 - 3cbdfaddd773415fad26acec8755ee5a:91e1f2d834cd4c7587a0691781018390:a68bb3449d4446a6b3d157cd702d721b

## 基本信息

- 查询日期：2026-06-09（北京时间）
- 命中环境：`llm-agnet`
- 用户输入：`冰箱运行模式设置为均衡`
- 路由任务：`复杂车控`
- 内部请求 ID：`d7a1a4b3-f9cb-468e-8238-a6b1a73f59af`
- 下游 DDS recordId：`cbe8aaef-53e2-478e-9644-ee0a6bd3b817`
- ERROR/WARN：未发现

## 结论

本次问题不是服务异常或超时，而是车控语义解析丢失了目标模式值。

上游将用户输入改写为 `冰箱切换均衡模式`，文本仍包含目标值“均衡”。但下游车控 NLU 只解析出：

```text
object=冰箱
action=切换
part=模式
```

最终生成的 `sys.car.crl` 命令参数中没有 `均衡` 对应的模式值，导致命令语义不完整。系统未校验必需槽位，仍返回 `status=0` 并发送 done 帧，因此表现为“收到，即刻安排”，但实际无法保证切换到均衡模式。

## 关键证据

1. `23:19:53.651` 收到用户输入 `冰箱运行模式设置为均衡`，任务为 `复杂车控`。
2. `23:19:53.652` 路由到 `carctl leapmotor`，立即发送开始提示 `收到，即刻安排。`
3. `23:19:55.066` 上游改写结果为 `冰箱切换均衡模式`。
4. `23:19:55.449` 下游 NLU 返回的槽位只有 `对象=冰箱`、`操作=切换`、`调节内容=模式`，缺少 `均衡`。
5. 最终命令为：

```json
{
  "api": "sys.car.crl",
  "param": {
    "object": "冰箱",
    "action": "切换",
    "part": "模式",
    "customInnerType": "nativeCommand"
  }
}
```

6. 下游返回 `status=0`、空 `speak`，服务随后直接发送 done 帧，没有真实执行结果确认。

## 耗时

| 节点 | 耗时 |
|---|---:|
| 首帧提示 | 1 ms |
| DFM 判断模型 | 866 ms |
| DDS 请求 | 382 ms |
| 总耗时 | 1798 ms |

耗时正常，不是性能问题。

## 修复建议

1. 为冰箱模式控制补充枚举槽位，将“均衡/母婴”等模式值解析并传入命令参数，例如 `mode=均衡`。
2. 在下发 `sys.car.crl` 前校验必需槽位；模式切换缺少目标值时不得按成功返回，应澄清或返回能力不支持。
3. 不要仅依据 DDS `status=0` 判定控制成功，增加车端执行 ACK 或状态回读。
4. 增加回归用例：`冰箱运行模式设置为均衡`、`冰箱切换均衡模式`、`设为母婴模式`。
