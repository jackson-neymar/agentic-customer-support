# 日志分析报告 - 746e612699234eff8aa3f36a9a2ee76f:a10c9a98a7ec4399887e421781018616:b231e464aa9d4d43a18f26b349d5b8de

## 基本信息

- 查询日期：2026-06-09（北京时间）
- 命中环境：`llm-agnet-e2e`
- 用户输入：`耳朵打开`
- 上游路由任务：`闲聊及百科大问答`
- 内部链路 ID：`0234cb9e-b7da-4c42-8110-365367c52cff`
- 请求 ID：`9a2e82e10aea46d6bb0cca53f98f2ad2`
- LLM 请求 ID：`ea8d88d6-57eb-41df-978c-2fe8115a0561`

## 结论

本次链路同时存在两个问题：

1. **意图路由错误**：用户说“耳朵打开”，结合连续的“关闭/折叠/展开后视镜”历史语境，应优先理解为“展开后视镜”。但上游将请求路由到 `闲聊及百科大问答`，E2E 闲聊模型误答为“已为您开启语音识别功能”，没有生成车控指令。
2. **WebSocket 断连后的流式任务未及时停止**：客户端在模型仍生成内容时关闭连接，服务端继续消费并发送 LLM 中间帧，发送时收到 WebSocket `1005`，最终 `llm_execute_and_reply` 和 `run_llm_task` 异常，任务被取消。

## 关键链路

| 北京时间 | 事件 |
|---|---|
| 23:23:37.397 | E2E WebSocket 建立 |
| 23:23:37.418 | 收到输入 `耳朵打开`，任务为 `闲聊及百科大问答` |
| 23:23:37.418 | 路由到闲聊百科 Agent |
| 23:23:37.422 | 成功发送 START 空帧 |
| 23:23:37.592 | LLM 首包返回，首包耗时 165 ms |
| 23:23:37.653 | 成功发送标签中间帧 `<neutral><zh>` |
| 23:23:37.702 | WebSocket 连接关闭，日志状态为正常 |
| 23:23:37.760 | 服务端继续发送“已为您开启语音识别功能”中间帧，发送失败：`1005 no status received` |
| 23:23:37.764 | `llm_execute_and_reply`、`run_llm_task` 报错 |
| 23:23:37.766 | Agent 因 WebSocket 关闭被取消 |

## 证据

### 意图理解错误

请求携带的历史上下文反复出现：

```text
关闭后视镜
折叠后视镜
展开后视镜
折叠后视镜
耳朵打开
```

但请求被路由到 `闲聊及百科大问答`，模型生成：

```text
已为您开启语音识别功能，
```

“耳朵”在该上下文中明显更可能是后视镜的口语指代，而非语音识别功能。

### WebSocket 发送失败

```text
23:23:37.702 socket_close ... status=正常
23:23:37.760 _send_to_websocket_fail_and_retry
e=received 1005 (no status received [internal]); then sent 1005
23:23:37.764 ERROR Error in llm_execute_and_reply
23:23:37.764 ERROR run_llm_task 执行异常
23:23:37.766 agent_canceled_with_websocket_close
```

## 影响

- 用户没有获得完整最终回复。
- 请求没有转成“展开后视镜”的车控命令。
- 服务端在连接关闭后继续进行无效的模型生成与发送。
- 异常日志未打印具体异常类型和堆栈，增加定位成本。

## 修复建议

1. 在意图分类和车控同义词中增加 `耳朵 -> 后视镜`，结合最近对话上下文将“耳朵打开”路由为“展开后视镜”。
2. 对连续车控对话增加上下文继承；当前句包含“打开”且上一轮对象为后视镜时，优先继承对象。
3. WebSocket 关闭后立即取消 LLM 流消费和并行任务，发送前检查连接状态，避免继续写入已关闭连接。
4. 将 `1005` 断连作为可预期取消处理，避免记录为空消息的 ERROR；同时记录异常类型、`repr(e)` 和堆栈。
5. 增加回归用例：在多轮后视镜操作后输入“耳朵打开”，应生成后视镜展开命令；客户端中途断连时任务应快速取消且不产生发送异常。
